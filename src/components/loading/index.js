import React from "react";
import "./style.css";
import images from "../../assets/images";

const Loading = props => {
	return (
		<div className="loading fixed-top"></div>
	);
};

export default Loading;